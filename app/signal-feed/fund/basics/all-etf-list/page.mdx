---
title: 全量 ETF 列表
description: 全量 ETF，无筛选；建议客户端缓存。
---

# 全量 ETF 列表

全量 ETF，无筛选；建议客户端缓存。

**HTTP 方法与路径**：`GET /api/v1/fund/all-etf-list`

## 入参

### Path

| 路径参数 | 无 |


### Query

| Query 参数 | 无 |


## 响应

> **响应外壳**：成功时 JSON 含 `success`、`data`、可选 `message`，见 [统一响应体](/signal-feed/api-reference#api-response-envelope)。

### 响应示例

对应请求 `GET /api/v1/fund/all-etf-list`。字段以线上为准。

```json
{
  "success": true,
  "data": "（该采样在缓存中无法解析为合法 JSON，常见原因为未转义换行或控制字符；文档不嵌入原文。请以真实请求或服务 OpenAPI /docs 为准。）"
}
```

## 请求示例

```bash
curl -sS -H "Authorization: Bearer <API_KEY>" "http://localhost:4000/api/v1/fund/all-etf-list"
```

