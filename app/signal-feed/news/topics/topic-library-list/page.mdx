---
title: 题材库列表
description: 题材库分类列表。
---

# 题材库列表

题材库分类列表。

**HTTP 方法与路径**：`GET /api/v1/news/topic/library/list`

## 入参

### Path

| 路径参数 | 无 |


### Query

| Query 参数 | 无 |


## 响应

> **响应外壳**：成功时 JSON 含 `success`、`data`、可选 `message`，见 [统一响应体](/signal-feed/api-reference#api-response-envelope)。

### 响应示例

对应请求 `GET /api/v1/news/topic/library/list`。字段以线上为准。

```json
{
  "success": true,
  "data": {
    "total": 80,
    "items": [
      {
        "id": 89,
        "name": "超节点服务器",
        "summary": "超节点作为国内各大云厂商、智算中心重点投入的新一代AI基础设施,在通信时延、算力密度、成本和效率、机房空间利用和TCO总成本等方面显著优于传统的GPU集群。",
        "tag": "",
        "limitUpNum": 0
      },
      "..."
    ]
  }
}
```

## 请求示例

```bash
curl -sS -H "Authorization: Bearer <API_KEY>" "http://localhost:4000/api/v1/news/topic/library/list"
```

