---
title: 海关进出口分页
description: 海关进出口分页
---

# 海关进出口分页

分页返回该指标表格数据。

**HTTP 方法与路径**：`GET /api/v1/macro/customs-trade`

## 入参

### Path

| 路径参数 | 无 |


### Query

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | undefined | undefined | undefined |
| `pageSize` | undefined | undefined | undefined |


## 响应

> **响应外壳**：成功时 JSON 含 `success`、`data`、可选 `message`，见 [统一响应体](/signal-feed/api-reference#api-response-envelope)。

### 响应示例

对应请求 `GET /api/v1/macro/customs-trade?page=1&pageSize=20`。字段以线上为准。

```json
{
  "success": true,
  "data": {
    "pages": 11,
    "data": [
      {
        "REPORT_DATE": "2026-03-01 00:00:00",
        "TIME": "2026年03月份",
        "EXIT_BASE": 321032652.74,
        "IMPORT_BASE": 269903560.06,
        "EXIT_BASE_SAME": 2.5,
        "IMPORT_BASE_SAME": 27.8,
        "EXIT_BASE_SEQUENTIAL": 7.07952771,
        "IMPORT_BASE_SEQUENTIAL": 29.12613796,
        "EXIT_ACCUMULATE": 977493896.094,
        "IMPORT_ACCUMULATE": 713160870.19,
        "EXIT_ACCUMULATE_SAME": 14.7,
        "IMPORT_ACCUMULATE_SAME": 22.7
      },
      "..."
    ],
    "count": 219
  }
}
```

## 请求示例

```bash
curl -sS -H "Authorization: Bearer <API_KEY>" "http://localhost:4000/api/v1/macro/customs-trade?page=1&pageSize=20"
```

